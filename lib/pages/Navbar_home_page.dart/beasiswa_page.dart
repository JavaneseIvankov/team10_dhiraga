import 'package:flutter/material.dart';

class BeasiswaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Beasiswa')),
      body: Center(child: Text('Daftar Beasiswa')),
    );
  }
}
