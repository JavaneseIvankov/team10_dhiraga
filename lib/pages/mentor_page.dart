import 'package:flutter/material.dart';

class MentorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Mentor")),
      body: Center(child: Text("Halaman Mentor")),
    );
  }
}
