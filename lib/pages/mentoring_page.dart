import 'package:flutter/material.dart';

class MentoringPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Mentoring")),
      body: Center(child: Text("Ini adalah halaman Mentoring")),
    );
  }
}
